import React from 'react';
import Header from '../../components/Header';

const Login = () => {
    return (
        <div>
            <Header/>
            <div>登录页面</div>
        </div>
    );
};

export default Login;
